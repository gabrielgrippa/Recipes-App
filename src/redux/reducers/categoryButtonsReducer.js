const initialState = {
  meals: [],
  drinks: [],
};

const categoryButtonsReducer = (state = initialState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default categoryButtonsReducer;
